package org.example;

public class Exemplu2 {

    public static void main(String[] args) {

        /*
        Predicate<T>      T x -> boolean
        BiPredicate<T,G>   (T x, G y) -> boolean
        Consumer<T>        (T x) -> void
        BiConsumer<T, G>    (T x, G y) -> void
        Function<T, R>      (T x) -> R
        BiFunction<T, G, R>  (T x, G y) -> R
        UnaryOperator<T>     (T x) -> T
        BinaryOperator<T>    (T x, T y) -> T
        Supplier<T>          () -> T
         */

        // Function<T,R>    (T x) -> R y
        // DoubleFunction<R>    (double x) -> R y
        // ToDoubleFunction<T>  (T x) -> double y
        // IntToDoubleFunction -> (int x) -> double y;


    }

}
